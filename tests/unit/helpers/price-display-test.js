import { priceDisplay } from 'bnb-db/helpers/price-display';
import { module, test } from 'qunit';

module('Unit | Helper | price display');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = priceDisplay([42]);
  assert.ok(result);
});
